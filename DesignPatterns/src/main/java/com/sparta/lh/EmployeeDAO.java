package com.sparta.lh;

import java.util.ArrayList;

public class EmployeeDAO {

    private ArrayList<EmployeeDTO> employeeDTOs = new ArrayList<>();

    // now provide ltos of static method to perform different types of operations

    // CRUD - creating, reading, updating, deleting

    // so put lots of static methods in here that give access to the array list.
    // so the DAO is a way to 'talk to' a collection
    // abstracts away how you talk to a collection
    // with a DAO you don't need to worry about SQL for example it's already taken care of here
}
